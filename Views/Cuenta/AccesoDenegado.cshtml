@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container mt-5 text-center">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="text-danger display-1 fw-bold"><i class="bi bi-shield-lock"></i> 403</h1>
            <h2 class="text-uppercase mt-4">Acceso Restringido</h2>
            <p class="text-muted mt-3">Lo sentimos, tu cuenta no tiene los permisos necesarios para ver esta página.</p>
            <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-4">Volver al Inicio</a>
        </div>
    </div>
</div>}
